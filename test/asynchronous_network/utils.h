#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <assert.h>
#include <string>

using namespace std;

void kvs_error(const char *, ...);

int max(int, int);

int min(int, int);

int hash(string &);

int flip(int);
